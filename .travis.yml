
language: python
python:
  - "3.7"
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python -m  unittest discover -s  src/CalculatorTests.py

branches:
  only:
    - master
    - development
